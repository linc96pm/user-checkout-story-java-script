export const checkIsString = (string) => typeof string === 'string' || string instanceof String;
export const checkIsNumber = (number) => typeof number === 'number' || number instanceof Number;
export const checkIsBoolean = (boolean) => typeof boolean === 'boolean' || boolean instanceof Boolean;
